import React from "react";
import "./download.css";

const DownloadButton = ({ logo, text, title }) => {
  return (
    <>
      <button>{logo}</button>
    </>
  );
};

export default DownloadButton;
